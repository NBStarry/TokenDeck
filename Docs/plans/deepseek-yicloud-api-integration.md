# DeepSeek 与易云 API 接入

- macOS 新增两个独立渠道，默认追加到现有渠道之后，沿用刷新间隔。DeepSeek 展示分币种总余额、充值余额和赠金；易云显示密钥查询状态、授权模型与暂不提供额度查询说明。
- 从 aimax Hermes 一次性通过 SSH 读取已有 Key，存入本机钥匙串，配置仅引用稳定渠道 ID；后续 Mac 直连，远端 Key 变化需重新导入。不修改 Hermes，不执行模型请求。
- 新增取数类型/API 信息模型，复用状态与缓存机制，relay 增量字段；保留旧配置、缓存与 PhanRouter 行为。不增加余额告警或手机端界面。
- 失败保留带时间的旧数据；认证、网络、结构错误使用固定提示，绝不输出原始响应或凭证。
- 测试解析、零值、多币种、空模型、失败、缓存兼容及导入去重；渲染检查、Swift 测试、Release 构建签名，加载本地验收版核对真实数据。未经单独授权不提交、发布或覆盖正式安装目录。

## 文档核对
实施后同步 README 的取数接口、凭证与使用方式，其他平台文档不改。

## 本地验证
- 36 项测试执行：35 项通过，1 项可选钥匙串测试跳过。涵盖零余额、多币种、异常数字、空模型、模型去重、认证/网络错误脱敏、导入去重/失败/冲突、缓存及 relay。
- 两张合成卡片已渲染检查，金额明细和查询状态无截断。新增渠道设置仅显示实际支持的字段；旧 PhanRouter 的余额和历史消耗模型未修改。
- README 已同步数据来源、钥匙串引用、标准输入导入、重试行为与不依赖 aimax 的运行方式。配置失败可能已写入部分钥匙串项，明确保留并允许重试，不声称跨文件与钥匙串原子事务。
- Release 构建和本地 app 签名校验通过。已通过 SSH 在进程内读取两个 Key，以标准输入导入本机钥匙串，未输出或落盘原始凭证；两个渠道已追加到用户配置。
- 签名 app 的真实 --fetch 验证：DeepSeek 成功返回 1 组币种余额；易云成功返回 3 个授权模型。查询在 Mac 直接执行，不依赖 aimax 运行时服务，无模型调用。
- 新版 GUI 已启动，但采样显示原有 Codex 账号名称读取停在 SecItemCopyMatching，relay 未监听。已请用户完成 macOS 钥匙串提示；真实仪表盘整体加载待核对，不能以 CLI 成功代替 UI 验收。
- 用户完成钥匙串授权后，运行中的 GUI relay 已恢复：3 个 Codex、PhanRouter、DeepSeek、易云全部状态 ok。DeepSeek 有 1 组币种余额，易云有 3 个授权模型；本地运行数据验证完成。真实界面视觉仍以用户查看为准，合成卡片已渲染检查。README 与最终行为一致，无需再改；未提交、发布或覆盖正式安装目录。
