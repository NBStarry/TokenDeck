# macOS 设置页修复与 Codex 多账户支持

## 已确认范围
- 设置页固定顶部完成按钮，正文整体滚动；仪表盘限制屏幕内高度并滚动，重新打开回到仪表盘。
- 支持任意数量账号（无三个账号上限）。分别在 Codex 登录后，显式添加／更新当前账号；支持备注与移除。
- 自动跟随 ~/.codex/auth.json 当前身份，当前账号置顶；每 5 秒检测，打开和刷新前同步。
- 凭证副本存入钥匙串，元数据无 token；身份、缓存、请求和告警冷却按账号隔离。
- 只有当前 Codex 和既有 Claude 等订阅渠道参与全局告警；非当前 Codex 仅展示。
- 请求最多四个并发；迟到结果不能恢复已移除账号或覆盖其他身份。
- 凭证过期提示重新登录后更新；不实现 OAuth 刷新，不改写 Codex 登录。
- 保持中转 JSON 兼容和 --fetch codex 当前账号语义，不修改 Tauri 客户端。

## 验证
- SwiftPM 隔离测试覆盖旧配置、零／一／三／十账号、去重、独立缓存、失效、移除、竞态、并发与告警。
- Release 编译、本地打包；小屏与长内容实际点击验证设置返回。
- 真实多账号验收依赖用户逐个登录添加，单独报告，不以模拟测试替代。

## 交付边界
不自动提交、推送、安装或部署，保留已有 AGENTS.md 改动。同步 README 和本计划。

## 状态
本地实现完成，已从独立构建目录运行供用户验收；未覆盖安装版本、提交或部署。

- `swift test --scratch-path .build/multi-account-validation`：15 项通过，覆盖计划中的账号数量、并发、竞态、告警、缓存及保存失败回滚。
- `swift build -c release --scratch-path .build/multi-account-validation`：通过。
- 独立组装并 ad-hoc 签名 `.build/multi-account-validation/TokenDeck.app`；`codesign --verify --deep --strict` 通过，未覆盖已有 app 产物或 /Applications 副本。
- 当前真实 Codex 的 `--fetch codex`：ok=true，解析到 1 个用量窗口；未输出凭证或原始响应。
- AppKit 离屏布局测试：360 × 480 点，十账号设置／仪表盘图已查看，顶部按钮与当前账号标识可见。生成图在同一独立构建目录。
- 宿主重启后 Computer Use 已恢复。480 点高、十账号的同组件隔离窗口已实际点击验证：保存备注、设置滚动到底后点击完成返回仪表盘、切换当前账号并置顶。
- 原生 NSPopover 子窗口的自动点击仍遇到 windowNotFoundAtPosition；隔离窗口通过不能替代菜单栏弹窗的完整现场验收。
- 多个真实账号的登录添加、钥匙串持久化交互和手机小组件重绑尚未验收，需用户分别登录并添加。
- README 已同步操作、存储、过期处理、告警范围、中转动态 ID 和旧截图标识；历史中转设计文档保留当时设计，不改写历史，当前兼容说明在 README。
- 原有 AGENTS.md 未修改。此卷大小写不敏感，Git 显示计划路径为 Docs/plans，与请求的 docs/plans 指向同一目录。


## Computer Use 超时排查（2026-09-21）

- 用户已要求修复验收工具超时。重置当前 CUA REPL 后，Finder 仍 120 秒超时；请求指定 20 秒也未覆盖底层服务超时。
- 只读核验系统 TCC：com.openai.sky.CUAService 的 Accessibility 和 ScreenCapture 均为已授权；Finder 在现有应用批准名单中。未修改任何权限或批准名单。
- 原辅助服务 PID 2466 自 9 月 14 日启动，采样有 ReplayKit recordingDidStopWithError 回调；这只能说明录屏相关异常线索，不能单独证明根因。
- 已定向 SIGTERM 该辅助服务；系统重新启动 PID 5313 并建立 computeruse.sock。重建 REPL 后 Finder 仍超时，随后应用枚举也超时，修复未成功。
- macOS log show 无法打开本机日志存储，桌面当日日志为空，限制了底层根因确认。诊断采样保留在 /tmp/tokendeck-sky-sample.txt 和 /tmp/tokendeck-sky-after-restart.txt。
- 下一步建议重启 ChatGPT/Codex 桌面宿主重建连接；宿主承载其他会话，已在当前会话请求用户决定，未擅自重启宿主。
- 飞书机器人身份已验证，但接收人查询未返回姓名或租户字段，不能完成规则要求的本人及租户核验，未发送通知。
- 本轮不修改 TokenDeck 代码；README 的功能和验收边界仍适用，无需为宿主故障调整产品说明。


### 宿主重启结果

- 用户明确批准后，确认 PID 1373 对应 ChatGPT 桌面宿主，使用 SIGTERM 正常结束，独立监督进程随后重新打开 /Applications/ChatGPT.app；未强制杀进程。
- 新宿主 PID 12709。重建 CUA REPL 后，Finder getApp 2.83 秒返回，后续 AX 读取 0.24 秒；实际点击 Finder 菜单成功（0.55 秒），已验证原生读取和点击恢复。
- 证据支持此前 120 秒超时与宿主侧状态／连接相关，不能进一步断言底层死锁机制。
- 无主窗口的 TokenDeckQA 菜单栏测试程序仍返回约 5 秒 timeoutReached，与 Finder 已恢复的宿主级故障分开记录；TokenDeck 实际点击验收仍未完成。
- 本轮没有修改产品代码或 README，现有功能说明仍适用；未安装新版 TokenDeck。测试程序已退出。


### 重启后继续验收

- `--preview-accounts` 增加可见隔离窗口，复用正式 PopoverRootView，并提供切换模拟身份和弹出面板入口；所有账号、缓存、配置写入和网络仍使用内存模拟，不访问真实钥匙串。
- 原生弹出面板可进入设置，但 Computer Use 无法定位部分子窗口按钮；未将此记为通过。独立窗口的真实点击完成了备注保存、长列表底部返回仪表盘和账号切换置顶，AX 状态及截图均已核对。
- 最新代码重新运行 15 项 SwiftPM 测试全部通过。README 已更新隔离窗口命令说明；功能、存储与手机兼容边界无需其他调整。
- 真实多账号登录及钥匙串持久化仍待用户配合；已在当前会话询问，尚未收到答复。飞书本人/租户身份仍无法完整核验，未发送通知。


### 真实账号验收准备

- 新增显式启用的 KeychainIntegrationTests：三组随机身份模拟凭证通过真实 Security API 完成保存、读回、token 更新、单独删除和最终清理，1 项通过；未读写用户已有凭证。默认单元测试跳过此项，README 已同步运行方式。
- 用户已答复现在配合逐个登录。已退出旧版 TokenDeck 和隔离 QA，从 `.build/multi-account-validation/TokenDeck.app` 启动最新 Release；未覆盖 `/Applications/TokenDeck.app`。
- 已请用户登录首个 Codex CLI 账号并操作添加及完成，真实账号取数、多账号保留、当前身份与提醒切换仍等待现场结果。

### 首个真实账号检查

- 用户确认当前 Codex CLI 即首个账号。实际 `--fetch codex` 返回 ok=true；运行中的新版中转快照显示 1 张 Codex 卡、状态 ok、位于首位。
- 只读检查配置仍为 0 个已保存账号；当前卡属于未保存的当前登录，不能证明保存和切换后的保留。已请用户在设置点击添加／更新并完成。
- 正式无主窗口应用的 Computer Use 连接仍 timeoutReached，无法代替用户完成该次实际点击；不通过直接改写配置冒充 UI 验收。等待用户操作后继续第二、第三账号和返回验证。
- README 功能和操作说明仍与实现一致，本次只补充现场证据，无需调整产品文档。

### 首账号已保存与备注反馈修正

- 配置现有 1 个已保存账号，中转快照同一账号状态 ok 且位于首位，有 1 个用量窗口。原生弹窗打开后 Computer Use 可连接，观察到仪表盘；无窗口时超时属于工具限制。
- 用户反馈备注旁保存没有提示，确认属于保存反馈缺失。补充保存成功的已保存提示、空备注/持久化失败提示，修改备注后清除旧反馈；仅持久化成功才显示成功。同步已有保存成功/失败测试断言。

- 15 项单元测试通过；默认跳过 1 项此前已显式通过的钥匙串集成测试。隔离 UI 实际点击确认已保存提示出现、继续编辑后消失；Release 重新构建和签名校验通过。已重启验收版加载修复，README 同步成功提示说明。

### 独立登录导入补充（用户已授权）

- 设置增加“从独立登录目录导入”，使用 macOS 文件选择器选择 Codex home 文件夹或其中 auth.json；只读取选定凭证，经现有身份校验、去重与钥匙串持久化流程添加／更新。
- 不修改默认或选定目录的登录文件，不调用 logout，不切换用户 Codex 会话；当前身份和全局 Codex 提醒继续跟随默认 ~/.codex/auth.json。不自动追踪独立目录的后续刷新，凭证失效后在对应目录重新登录再导入。
- 验证目录/文件导入、同身份更新与备注保留、坏文件不落盘、配置失败回滚、导入不切换当前账号与告警；核对原生选择器和成功反馈，重新构建验收版。README 同步独立目录登录命令及使用影响。

- 17 项单元测试通过（真实钥匙串集成测试本轮默认跳过，此前已独立通过）。原生文件选择器实际选中模拟登录目录，导入后显示成功提示并增加第 11 个账号，当前账号保持原身份。目录/JSON 文件读取和失败场景由测试覆盖。
- Release 构建与签名校验通过；已重启独立构建目录的验收版，未覆盖安装版本。真实独立登录导入仍需用户完成浏览器登录后验收。
- README 已核对并同步目录命令、选择器操作、去重、当前身份规则和凭证副本需手动更新的限制。

### 三账号现场复核

- 用户回复操作完成后，只读检查配置中已有 3 个账号。原验收进程及会话句柄均不存在，重新启动同一签名构建验证重载。
- 运行快照显示 3 张 Codex 卡，默认登录身份位于首位；2 个状态 ok，1 个状态 stale 且错误为凭证过期。账号持久化和当前置顶已验证，不能声称全部账号实时取数通过。
- 已询问本次是否新增第三账号或更新首账号，以及顶部完成返回的实际结果；需要更新剩余失效账号后继续验收。README 已有过期与重新导入说明，无需改动。

### App 按钮登录与自动命名实施
- 添加账号及重新登录在独立管理窗口中发起官方 Codex 浏览器登录；独立临时 CODEX_HOME，不退出或改写默认登录。单任务、取消、五分钟超时、失败重试与持久化重试。
- 自动发现 CLI，缺失可选程序及打开官方安装说明；后台进程不输出认证日志。成功校验身份、去重、保存钥匙串并刷新，重登不允许替换为其他身份。
- 官方 name 优先，profile name / 邮箱回退，缺失用短身份摘要。移除自定义备注 UI，兼容旧凭证及配置；同名追加短摘要。
- 测试登录进程生命周期、失败/取消/超时、身份不匹配、持久化失败、名称兼容、默认登录不变；构建签名与真实浏览器验收分开报告。

- 本轮实现独立登录窗口、官方 CLI 自动定位（npm 安装解析到原生程序）、五分钟超时、取消、临时目录清理、匹配原身份重登及保存重试。移除自定义备注，现有账号显示从凭证计算官方名称，新增/更新时同步元数据名称。
- 21 项相关测试通过：完整套件 20 项通过、1 项可选钥匙串测试跳过，随后新增身份不匹配测试单独通过。登录生命周期使用隔离假程序真实运行，未发起真实账号登录。Release 构建与签名校验通过，已加载本地验收版。
- README 已改为按钮登录主流程；保留辅助导入与默认身份/手动续期边界。原先用户已确认“完成”可返回仪表盘。
- 正式应用无可见窗口时 Computer Use 仍超时，尚未完成本次浏览器登录的用户验收；已请用户点击添加或重新登录，无需命令行。未提交、发布或覆盖 /Applications 安装版本。

### 最终本地验收结果

- 用户确认通过 App 按钮完成浏览器登录。运行快照中仍为 3 个已保存账号、3 张账号卡，全部状态 ok，原过期账号已恢复，未产生重复账号。
- 对照默认登录身份校验当前账号位于第一张卡；当前账号标题与官方凭证名称一致。未输出凭证、姓名或原始响应。
- 设置完成返回仪表盘由用户此前明确确认；账号数不设上限、告警仅跟随当前 Codex、并发/竞态/错误隔离通过相关自动测试。登录取消/超时/进程清理、名称兼容与错误身份拒绝已有测试证据。
- 本地实现、Release 构建签名及用户参与的真实登录验收完成。README 已核对，与按钮登录及手动重新登录行为一致。未提交、推送、发布或覆盖 /Applications；手机端现场验收不属于此次 Swift 修改交付。
- Computer Use 在无可见窗口时仍存在连接超时，本次登录完成依据用户反馈与运行数据共同验证，不将工具连接成功作为产品验收前提。

### CLI 当前账号切换（已授权实施）
- 每账号提供设为 CLI 当前账号，保存完整登录到钥匙串；旧凭证先在 App 重新登录再切换。保存切离账号最新凭证，原子写默认 auth.json 并验证身份；文件外部变化中止，回滚不覆盖后续外部写入。
- 不退出既有会话、不运行 logout；成功提示重开 CLI，当前置顶与告警同步。仅默认目录文件认证；keyring/受管理/无法确认设置拒绝，登录和切换互斥。
- 隔离测试 A/B 往返、最新凭证保存、失败与并发修改、权限、旧格式兼容；更新 README，构建签名后由用户真实验收，不提交或安装。

- 实现保存完整凭证、原当前登录凭证同步、原子替换与读回验证、旧账号重新登录后继续切换、登录/切换按钮互斥。默认凭证不完整或非文件认证配置拒绝切换。
- 24 项完整测试通过、1 项可选钥匙串测试跳过；新增 Store 切换排序与告警测试单独通过（切换组 4 项全过）。测试均使用隔离目录，不改动真实默认账号。
- 失败策略：原子替换前失败保留原文件；替换后检测外部变化保留外部结果，不强制回滚。外部 Codex 不共享锁，读比对与 rename 之间无法保证无竞争；README 明确该限制及重开会话提示，不声称跨进程严格事务。

- Release 构建与签名校验通过，已重启本地验收版加载按钮；未修改默认登录，真实 A/B 切换由用户点击验证。README 已同步操作、完整凭证存储和会话影响。未提交、发布或覆盖安装版本。

- 用户明确反馈 CLI 往返切换成功。只读复核：3 个已保存账号全部取数 ok，默认登录包含完整 CLI 凭证，仪表盘第一张卡身份与默认登录一致。本次切换功能的用户验收完成；README 操作与限制仍适用，无需追加修改。仍仅运行本地验收构建，未提交、发布或覆盖安装版本。

### 周额度与重置机会明细
- 根据接口 limit_window_seconds 识别实际周/5 小时窗口，保持对应显示开关与告警语义；不把 primary 固定解释为 5 小时。
- 只读获取重置机会明细，显示剩余次数、当前套餐适用次数及每次机会的本地到期时间；不提供或调用消耗操作。
- 明细失败保留用量与摘要次数，明确到期明细不可用；缓存与 relay 增量保存新字段；旧 Codex 错误窗口缓存不再复用，其他渠道缓存兼容。
- 添加解析、失败回退与缓存测试，执行 Swift 测试和 Release 构建；同步 README。本轮不安装或发布，真实 UI 验收另行报告。

- 31 项测试执行：30 项通过、1 项可选钥匙串测试跳过。新增周 primary、双窗口、未知窗口拒绝、重置历史过滤/到期排序、明细失败与零次区分、缓存迁移和 relay 时间格式测试。合成账号卡片已渲染检查，无截断。
- README 能力、数据来源及缓存迁移说明已同步；不改 Tauri 实现，不需要 Windows/Android 验收。
- Release 构建和本地 app 签名校验通过，已启动新版验收进程。启动采样显示主线程停在读取已保存账号的 SecItemCopyMatching，relay 尚未监听；可能在等待 macOS 钥匙串授权，已请用户检查系统提示。未将进程存在报告为新版运行验收通过。
- 本次真实只读接口已确认当前账号为周窗口，明细含 2 次 available 机会及各自 expires_at。新版三个账号运行核对待系统钥匙串读取恢复；未提交、发布或覆盖 /Applications。
- 用户完成钥匙串授权后，新版 relay 已恢复。只读运行核对：3 个 Codex 账号均 ok，实际均显示周窗口；首位账号剩余 2 次且两条到期时间齐全，另两个账号均剩余 0 次。新版取数与运行验证完成，真实界面视觉仍以用户查看为准；合成卡片已通过渲染检查。README 与最终行为一致，无需再改。
